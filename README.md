1 - O retorno NaN é uma propriedade que significa "Not a Number". Por exemplo quando o código pede uma entrada de número e o usuário digita letras.

2 - O CSS pode ficar inline, ou seja, dentro de cada linha do HTML. Pode ser interno, dentro do head a partir da tag style ou externo, onde código fica em um arquivo .css separado e é ligado ao HTML por uma linkagem.

3 - .card-produto {
  width: 300px;
  margin-left: auto;
  margin-right: auto;
}

4 - As listas ordenadas (ol) organizam o conteúdo em listas com numeração, por exemplo:
1. Pão
2. Macarrão
3. Arroz

    Já as listas não ordenadas (ul) organizam o conteúdo em uma lista não numerada, por exemplo:
. Pão
. Macarrão
. Arroz

5 - A propriedade display vai definir o comportamento da div em CSS.

6 -  A box contém o conteúdo, preenchimento, borda e a margem. E esses itens são como o motor de renderização do navegador representa cada elemento do CSS.

7 - As tags semânticas são códigos responsáveis por substituir as divs em ocasiões específicas, podendo ser personalizadas, que poderiam ser feitas pelas divs, mas geraria uma dificuldade maior. Temos com exemplo o:
<header>: Responsável por gerar um cabeçalho na página
<section>: Cria uma seção/caixa no container principal
<article>:
<footer>: Gera um rodapé na página

8 - 

9 - A propriedade flex é utilizada para organizar os conteúdos dentro de uma DIV. A organização depende do que o programador decidir, podendo ser usado em coluna, horizontal e etc. 

9 - A propriedade flex é um comando atribuído ao contêiner pai, que vai determinar a organização dos elementos filhos (Div's); podendo alterar posições e ordens dos elementos.
10 -
11 -
12 - 
13 -
14 -

15 - 

17 - O Java é uma linguagem muito robusta usada para criar aplicativos completos e sistemas grandes, enquanto o JavaScript serve principalmente para deixar páginas da web dinâmicas e interativas, sendo aceita por todos os navegadores.

18 -

19 - Operadores lógicos são tags que vão verificar determinados requisitos que o usuário pedir em 2 ou mais variáveis/valores. Por exemplo, em um if, o operador lógico && vai ampliar a checagem de fatos de 1 fato para 2, funcionando como um "E", onde o if tem que atender a duas demandas para ser verdadeiro, caso atenda só a uma, irá cair no "else"

20 - Sim, basta digitar o nome do objeto e em seguida, a propriedade desejada, atribuindo um valor à ela.
Ex: Objeto.propriedade = "valor desejado"


21 - Null vai determinar que a variável tem valor nulo propositalmente. Já o undefined, ocorrerá quando a variável não possuir um valor determinado.

22 - Um objeto em JavaScript é uma coleção de dados e funções criada como pares de chave e valor. Ele é declarado usando chaves {}, com propriedades dentro.
-- ex: 

var meuCarro = new Object ();
meuCarro.fabricacao = "Ford"
meu Carro.modelo ="Mustang";
meuCarro.ano ="1969"
